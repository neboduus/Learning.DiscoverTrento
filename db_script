create table photo(
    id serial not null primary key,
    url varchar(200) not null);

create table place(
	id serial not null primary key,
	name varchar(50) not null,
	address varchar(200) not null,
	lat double precision not null,
	long double precision not null,
	history text not null,
	info text,
	type smallint,
    photo_id serial references photo(id));

create table event(
	id serial not null primary key,
	name varchar(50) not null,
	address varchar(200),
	data varchar(20) not null,
	hour_range varchar(20) not null,
	description text not null,
	cost smallint not null,
	place_id serial references place(id),
	type smallint);

create table news(
	id serial not null primary key,
	description text not null,
	data varchar(20) not null,
	hour varchar(20),
	place_id serial references place(id));
    
create table admin(
    username varchar(20) primary key,
    password varchar(20));

insert into photo(url) values('/img/discover_icon.png');

insert into admin(username, password) values ('nepotu', 'calculator');

insert into place(name, address, lat, long, history, info, type, photo_id)
	values('Piazza Dante', 'Trento Centro', 0, 0, 'storia breve', 'aperto tutti i giorni', 0, 1);
